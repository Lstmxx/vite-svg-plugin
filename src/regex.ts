export const findSvgTitleReg = /<svg([^>+].*?)>/;

export const findHeightWidthReg = /(width|height)="([^>+].*?)"/g;

export const findViewBoxReg = /(viewBox="[^>+].*?")/g;

export const findReturnStrReg = /(\r)|(\n)/g;
